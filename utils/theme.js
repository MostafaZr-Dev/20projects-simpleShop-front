import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  fonts: {
    heading: "vazir,open-sans",
    body: "vazir,open-sans",
    text: "vazir,open-sans",
  },
});
export default theme;
